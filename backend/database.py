# database.py
# add_user(user_data) → stores new user info

# get_all_users() → returns list of user dicts

# get_users_by_crn(crn) → returns users interested in a specific class

# You can use:

    # A JSON file (for prototyping)

    # SQLite/PostgreSQL (for production)